APP_NAME=Atally

# ===== 環境設定 =====
# 本番環境では必ず以下を設定:
#   APP_ENV=production
#   APP_DEBUG=false
#   APP_KEY= (php artisan key:generate で生成 — 本番デプロイ前に必ず再生成)
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=Asia/Tokyo
APP_URL=http://localhost:8000

# ===== ログ =====
LOG_CHANNEL=stack
# 本番: warning 推奨（debug だとパフォーマンス低下＋情報漏洩リスク）
LOG_LEVEL=warning

# ===== データベース =====
DB_CONNECTION=pgsql
DB_HOST=db
DB_PORT=5432
DB_DATABASE=atally
DB_USERNAME=atally
# 本番: 32文字以上のランダムなパスワードを設定
DB_PASSWORD=

# ===== キャッシュ / セッション / キュー =====
BROADCAST_DRIVER=log
CACHE_DRIVER=redis
FILESYSTEM_DISK=local
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120

# ===== Redis =====
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# ===== メール =====
# 本番: smtp に変更し、実際のSMTP情報を設定
#   例: Amazon SES, SendGrid, Mailgun 等
MAIL_MAILER=smtp
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@atally.jp"
MAIL_FROM_NAME="${APP_NAME}"

# ===== Stripe 決済 =====
# Stripe ダッシュボードから取得（本番キーを使用）
STRIPE_KEY=
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=

# ===== Google OAuth =====
# Google Cloud Console から取得
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL="${APP_URL}/auth/google/callback"

# ===== CORS =====
# 本番: 許可するオリジンをカンマ区切りで指定
# CORS_ALLOWED_ORIGINS=https://atally.jp,https://www.atally.jp

# ===== Sanctum =====
# 本番: ステートフルドメイン
# SANCTUM_STATEFUL_DOMAINS=atally.jp,www.atally.jp
# トークン有効期限（分）: デフォルト30日
SANCTUM_TOKEN_EXPIRATION=43200

VITE_APP_NAME="${APP_NAME}"

# ===== ハローワーク API =====
# 厚生労働省 提供求人API（申請して取得）
# トークンは当日のみ有効。メンテナンス: 毎日 0:00-6:00、月末 21:30-6:00
HELLOWORK_BASE_URL=
HELLOWORK_ID=
HELLOWORK_PASS=
